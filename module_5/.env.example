# Runtime — least-privilege app_user (SELECT, INSERT, UPDATE only)
# Use Option 1 OR Option 2, not both.

# Option 1: DATABASE_URL (12-factor standard)
DATABASE_URL=postgresql://app_user:change_me@localhost:5432/applicant_data

# Option 2: individual variables (used when DATABASE_URL is not set)
DB_NAME=applicant_data
DB_USER=app_user
DB_HOST=localhost
DB_PORT=5432
DB_PASSWORD=change_me

# Admin database for initial setup (used by load_data.py to create the target database)
DB_ADMIN_NAME=postgres
